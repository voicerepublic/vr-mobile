describe 'Unit Tests: Login', ->
	#feature
	describe 'user login form', ->
	  # critical units
	  it 'ensure invalid email addresses are caught', ->
	  it 'ensure valid email addresses pass validation', ->
	  it 'ensure submitting form changes path', ->
	  # nice-to-have units
	  it 'ensure client-side helper shown for empty fields', ->
	  it 'ensure hitting enter on password field submits form', ->