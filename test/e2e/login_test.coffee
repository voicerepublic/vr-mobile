describe 'Integration/E2E Testing: login', ->
	
	it 'ensures user can log in', ->
		# expect current scope to contain username

	it 'ensures path has changed', ->
		# expect path to equal '/tabs/record'